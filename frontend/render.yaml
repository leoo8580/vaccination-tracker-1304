services:
  - type: web
    name: vaccination-tracker-frontend
    env: static
    buildCommand: chmod +x render-build.sh && ./render-build.sh
    publishDir: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html